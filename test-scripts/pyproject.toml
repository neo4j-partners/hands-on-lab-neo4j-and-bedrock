[project]
name = "neo4j-lab-verification"
version = "0.1.0"
description = "Scripts to verify Neo4j Lab 2 Aura Agents instructions"
requires-python = ">=3.10"
dependencies = [
    "neo4j>=5.0.0",
    "python-dotenv>=1.0.0",
]

[dependency-groups]
dev = []
